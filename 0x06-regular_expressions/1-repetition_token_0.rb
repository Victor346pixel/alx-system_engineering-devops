#!/usr/bin/env ruby
#
#def validate_string(input_string)
#  pattern = /^[A-Z]+[a-z]+-[0-9]+$/
#    if input_string.match?(pattern)
#        puts "Valid input: #{input_string}"
#          else
#              puts "Invalid input: #{input_string}"
#                end
#                end
#
#                # Check if the script is run with an argument
#                if ARGV.length != 1
#                  puts "Usage: ruby script_name.rb <input_string>"
#                  else
#                    input_string = ARGV[0]
#                      validate_string(input_string)
#                      end
#
